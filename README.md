# supervision-js

Private prototype workspace for internal planning and early browser rendering
experiments.

Current planning docs live in [`docs/internal`](docs/internal). Public-facing
README polish is intentionally deferred until the rendering foundation is proven.
