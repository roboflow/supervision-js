node_modules/
dist/
artifacts/
docs/site/
coverage/
.vite/
.expo/

examples/react-native/.expo/
examples/react-native/build/
examples/react-native/ios/
examples/react-native/android/

# Android library module build outputs (Gradle/CMake)
packages/react-native/android/build/
packages/react-native/android/.cxx/
.gradle/
local.properties

*.tsbuildinfo
.eslintcache
__pycache__/
*.py[cod]
tools/sam3-fixture/output/
tools/geometry-fixture/output/

# Build intermediates the demo never reads; npm run fixture:sam3:restore
# rebuilds them from the committed raw-sam3.jsonl captures
demo/fixtures/basketball_sam3/detections.json
demo/fixtures/horse_trail/detections.json

npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

.DS_Store

.env
.env.*
!.env.example


# Written by an eval run. A baseline is one machine's numbers, so it stays local.
tools/demo-eval/report.json
tools/demo-eval/baseline.json

# Not committed, and never has been. Present only so `prettier --check .`
# does not walk it; prettier reads this file and not .git/info/exclude.
local_claude/
