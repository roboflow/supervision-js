# Basketball Source Media

This folder keeps the original `basketball_sample.mp4` source media used by the
internal SAM3 fixture tooling.

Rapid inference events and derived Rapid detections have been removed from the
demo. The demo fixture lives in `../basketball_sam3/` and pairs the normalized
WebM media with SAM3 detections generated from frames extracted out of that same
WebM.
